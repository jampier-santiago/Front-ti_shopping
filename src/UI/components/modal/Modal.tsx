// Packages
import { FC } from "react";

import useStyles from "./modal.styles";

const Modal: FC = () => {
  // Styles
  const {} = useStyles();

  return <section></section>;
};

export default Modal;
